{
  "name": "@callit/core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "DeepBook Predict bindings, PredictService business logic, indexer client",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "codegen": "sui-ts-codegen generate",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "cli": "tsx cli/callit.ts"
  },
  "dependencies": {
    "@mysten/sui": "^2.17.0",
    "@mysten/bcs": "^2.0.5"
  },
  "devDependencies": {
    "@mysten/codegen": "^0.10.6",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}